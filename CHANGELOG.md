# v0.0.8c
- 新增`/swc`指令：查看功能启用状态
- 为`/setip`增加了说明与报错
- 为`/dice`和`/ddice`功能增加了开关，但是并没有写进对应的功能————谁会关这个呢？  
（下次大更或许会加进去）
# v0.0.8b
- 输入`/ff picture`时会输出使用提示而非报错
- 为`/sw`指令添加了描述文本
# v0.0.8a
这是首次记录，因此把目前为止的所有功能都写在这里
- 文生图  
调用本地ComfyUI进行绘图。插件中是一个简单的 Krea2-INT8 工作流。
- 扔骰子  
扔任意数量、任意面数的骰子。支持简洁（仅最终结果）和详细（包括每一颗骰子的结果）显示，是两个不同的指令。

好像也就这么多了......总之先这样吧。毕竟只是一个简单的记录。